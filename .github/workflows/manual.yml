on:
  workflow_dispatch:
    inputs:
      environment:
        description: "choose environment"
        required: true
        default: "staging"
        type: choice
        options:
          - staging
          - production
jobs:
  deploy-app:
    runs-on: ubuntu-latest
    steps:
      - name: deploy app
      - run: echo "deploy app mohamed khaled"
        
