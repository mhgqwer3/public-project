name: version1 

on:
  push:
    branches:
      - main
      
jobs:
  example-job:
    runs-on: ubuntu-latest
    steps:
      - name: print a welcome massage
        run: echo "welcome github action"
