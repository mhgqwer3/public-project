apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: smart-bins-monitor
  namespace: monitoring
  labels:
    release: prometheus
spec:
  namespaceSelector:
    matchNames:
      - smart-bins
  selector:
    matchLabels:
      app: backend
  endpoints:
    - port: http
      interval: 15s
